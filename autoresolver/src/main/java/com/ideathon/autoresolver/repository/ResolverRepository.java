package com.ideathon.autoresolver.repository;

import com.ideathon.autoresolver.entity.Resolver;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResolverRepository extends JpaRepository<Resolver,String> {
}
